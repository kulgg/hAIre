---
import Layout from "./Layout.astro";
import MenuItems from "./MenuItems.astro";

export interface Props {
	title: string;
	stakeholder: string;
}

const { title, stakeholder } = Astro.props;
---

<Layout title={title}>
	<MenuItems stakeholder={stakeholder} slot="menu-items" />
</Layout>
